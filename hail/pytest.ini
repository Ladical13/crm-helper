[pytest]
testpaths = tests
markers =
    live: hits the real NOAA S3 bucket; deselected by default so the suite
        stays offline and deterministic. Run with `pytest -m live`.
addopts = -m "not live"
