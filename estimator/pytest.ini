[pytest]
testpaths = tests
# app.py's datetime.utcnow() calls are deprecated but not this suite's problem;
# keep the signal-to-noise high so real failures stand out.
filterwarnings =
    ignore::DeprecationWarning
